# 척척 보안 원칙

## 현재 위협 모델

척척은 GitHub Pages에서 실행되는 정적 웹앱이다. 서버, 로그인, 데이터베이스, 파일 업로드, 결제 기능이 없으며 프로젝트 정보는 사용자의 브라우저 `localStorage`에만 저장된다.

- 실행 코드는 동일 출처의 저장소 파일만 허용한다.
- 외부 네트워크 요청, 프레임, 플러그인 객체, 폼 전송은 기본 차단한다.
- 새 창 링크는 HTTPS만 허용하고 `noopener noreferrer`와 `no-referrer`를 적용한다.
- 문자열을 코드로 실행하는 `eval`과 `new Function`을 사용하지 않는다.
- 사용자·저장 데이터는 HTML에 넣기 전에 이스케이프하며 자유 입력 길이를 제한한다.
- 프로젝트 메모에는 고객 개인정보, 계약정보, 비공개 도면 링크 등 기밀정보를 입력하지 않는다. `localStorage`는 암호화 저장소가 아니다.

## 배포 및 계정

- 저장소 소유자는 GitHub 2단계 인증과 복구 수단을 유지한다.
- 외부 JavaScript/CSS CDN 의존성을 추가하지 않는다.
- 변경 전 정적 보안 검사와 기능 회귀 테스트를 통과시킨다.
- GitHub 계정이나 저장소가 탈취되면 사이트 코드도 바뀔 수 있으므로 계정 보안이 최우선 방어선이다.

## 알려진 한계

정적 웹사이트는 사용자의 운영체제·브라우저·네트워크에 이미 존재하는 악성코드나 랜섬웨어를 탐지·제거할 수 없다. 척척은 다운로드·업로드·백엔드 처리와 외부 스크립트를 두지 않아 사이트 자체가 악성코드 전달 경로가 될 가능성을 줄인다.

GitHub Pages에서는 임의 HTTP 응답 헤더 설정이 제한된다. 현재 가능한 CSP는 문서의 `meta` 정책으로 적용하며, 클릭재킹 방지용 `frame-ancestors` 같은 응답 헤더 수준 정책은 향후 헤더 제어가 가능한 호스팅으로 이전할 때 적용한다.

## 구조 원칙

- 기능 라우팅, 화면 렌더링, UI 장식, 보안을 서로 다른 책임으로 유지한다.
- 하나의 상호작용에는 하나의 최종 이벤트 관리자를 둔다.
- `MutationObserver`는 DOM 추가·삭제만 관찰하고 자신이 변경한 클래스·스타일을 다시 관찰하지 않는다.
- 모든 렌더 함수는 같은 입력에 같은 DOM 상태를 만드는 멱등성을 가져야 한다.
- 새 패치 파일을 무기한 추가하지 않고 안정화 단계에서 분야별 모듈로 통합한다.
