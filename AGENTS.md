# 척척 개발 기준

사용자가 지정한 통합 개발 원칙이다. 이 저장소를 수정하는 모든 개발자는 먼저 읽는다.

## 우선순위

보안 → 기존 기능 안정성 → 데이터 무결성 → 사용자 요청의 정확한 구현 → 유지보수성 → 코드 가독성 → UI 일관성 → 성능 → 개발 편의성.
속도나 코드 미관 때문에 앞선 기준을 희생하지 않는다.

## 작업 시작과 범위

- 실제 원하는 결과와 확인 가능한 성공 조건부터 정한다. 코드 작성 자체를 완료로 보지 않는다.
- 관련 파일·함수·컴포넌트·데이터·연결 인터페이스와 공유 사용처를 확인한다. 권한·보안·기존 데이터 호환성·기능 영향을 검토한다.
- 필요한 범위부터 읽고 실제 의존성이 확인될 때만 넓힌다. 기존 패턴과 더 작은 수정으로 해결 가능한지 먼저 판단한다.
- 명확한 요청은 불필요한 질문 없이 진행한다. 요청 밖 기능·전면 재작성·대규모 리팩토링·미래용 확장 구조를 추가하지 않는다.
- 독립적인 변경을 무리하게 묶지 않는다. 구조 확인 → 성공 조건 → 최소 변경 → 기능 검증 → 회귀 검사 → 문제 해결 → 다음 단계 순서로 진행한다.
- 기존 정상 기능·인터페이스·저장 데이터를 보존한다. 동작 변경이 필요하면 이유와 영향 범위를 명시한다.
- 범위 밖 개선은 별도로 보고한다. 긴급 보안 문제의 예외 수정은 이유와 영향 범위를 설명한다.

## 구조와 가독성

- 기존 프로젝트 패턴을 우선하며 UI·입력·업무 판단·데이터·상태·통신·보안·로그·오류 처리의 책임을 필요한 만큼만 구분한다.
- 새 모듈은 `src/content`, `domain`, `application`, `infrastructure`, `ui`, `styles`의 기존 경계를 따른다. `app/`은 브라우저 연결 진입점이다.
- 제3자가 역할을 알 수 있는 이름을 사용한다. 의미 없는 약어·숨겨진 의존성·설정 중복·과도한 추상화·클래스 계층·파일 분할을 피한다.
- 긴 함수나 복잡한 조건은 이해에 도움이 되는 경우 분리한다. 짧은 코드보다 이해 가능한 코드를 우선한다.
- 주석은 코드의 반복 설명보다 업무 규칙·예외·보안 조건·비일반적 선택의 이유를 설명한다.
- 라이브러리는 기존 코드와 설치된 의존성으로 해결되지 않을 때만 검토한다. 크기·유지보수·알려진 취약점·호환성을 확인하고 추가와 버전을 기록한다. 주요 버전을 임의로 바꾸지 않는다.
- 성능은 실제 문제를 측정하고 병목·목표를 정한 뒤 최소 수정한다. 전후 측정과 가독성·메모리 등 부작용을 확인한다.

## 보안·데이터·오류

- 사용자 입력과 외부 데이터를 신뢰하지 않는다. 해당 기능에 맞게 타입·길이·허용 값·파일 형식/크기·경로·요청 범위·권한을 검증한다.
- 비밀번호·키·토큰·인증정보·개인정보·사내 민감정보를 코드에 직접 넣지 않는다. 필요한 경우 환경변수 또는 적절한 보안 저장소를 사용한다.
- 로그에 인증정보·개인정보·이메일 전체 본문·불필요한 프로젝트 정보·민감한 원본을 남기지 않는다.
- 사용자 오류 안내에 내부 경로·서버/DB 구조·인증정보·상세 스택을 노출하지 않는다. 개발자 진단과 사용자 안내를 구분한다.
- 실제 필요한 데이터와 시스템에만 접근한다. UI 숨김으로 권한 검사를 대신하지 말고 데이터 접근 단계에서 검사한다.
- 데이터 손상 위험이 있는 변경을 함부로 하지 않는다. 손상되거나 미지원인 저장값을 자동 초기화하여 덮어쓰지 않는다.
- 실패를 성공으로 반환하거나 무조건 예외를 삼키지 않는다. 안전한 상태를 유지하고 필요한 안내와 민감정보 없는 진단을 제공한다.
- 버그는 예상/실제 동작 → 재현 조건 → 코드 추적 → 근본 원인 → 최소 수정 → 재현 재검사 → 회귀 순으로 해결한다. 필요하면 재발 방지 검사를 추가한다.
- 테스트 통과를 위해 기능을 삭제·우회하거나 오류를 숨기지 않는다. 검사 실패를 숨기려고 기존 파일 해시 예외를 일괄 갱신하지 않는다.

## UI

- 한 화면의 정보량을 낮게 유지하고 여백과 글의 위계를 명확히 한다. 사용자가 지금 해야 할 다음 행동을 알 수 있어야 한다.
- 핵심 행동과 보조 행동을 구분한다. 기능이 늘어도 한 화면에 모두 노출하지 않는다.
- 기존 UI 언어를 유지한다. 작은 추가 때문에 전체 화면을 재설계하지 않는다.
- 수준별 상세 정보와 성취를 구분한다. 높은 레벨에서도 낮은 정보 깊이로 볼 수 있어야 한다. 필수 주의사항은 숨기지 않는다.

## 위험도에 맞는 검증

- 단순 UI: 화면·인접 요소·기존 조작 확인.
- 기능: 정상 흐름·잘못된 입력·반복 조작·연결 기능 확인.
- 데이터: 정상·빈 값·최소/최대·긴 입력·특수문자·중복·잘못된 형식·예상 밖의 데이터 확인.
- API/파일/인증/권한/메일: 정상/무권한 접근·실패 응답·네트워크 오류·잘못된 입력·허용 범위 밖 접근·민감정보 노출·데이터 손상 가능성 확인.
- 중요한 기능은 정상(Happy Path)·경계(Edge Case)·실패(Error Case)를 검토한다.
- 관련 회귀 범위에는 메인·검색·질문·분야/레벨 선택·Algorithm Tree·조건·결과·빈 데이터·잘못된 입력·권한 제한을 포함한다. 아직 구현되지 않은 기능을 검증 완료로 표시하지 않는다.
- 기본 검사: `npm run check`. 브라우저 통합 검사: `npm run check:browser`. 배포 산출물: `npm run build:site`. 수정 위험에 따라 필요한 검사를 선택하고 실행 결과를 기록한다.
- 실패 원인을 해결한 후 다음 단계로 넘어간다. 테스트가 통과해도 실제 사용자 결과와 기존 동작을 확인한다.

## 완료 기준과 보고

완료 전에 요청/성공 조건·최소 범위·기존 기능·정상/경계/실패·보안·권한·민감정보·구조 일관성·제3자 이해 가능성·불필요한 의존성과 복잡성·오류 은폐 여부를 확인한다. 중요한 문제가 있으면 완료라고 하지 않는다.

보고는 다음 항목으로 간결하게 작성한다.

- **변경**: 실제 변경한 기능.
- **영향 범위**: 주요 파일·컴포넌트·기능.
- **유지**: 의도적으로 바꾸지 않은 기존 기능.
- **검증**: 실제 수행한 검사와 회귀 결과. 과거 결과와 이번 실행을 구분한다.
- **보안**: 보안·권한 변경 여부와 확인한 범위.
- **남은 사항**: 제한·미검증 부분·별도 개선. 로컬 구현과 원격 반영/배포를 구분한다.

마지막으로 묻는다. “이 변경은 사용자의 요청을 충족하면서도 척척을 이전보다 더 불안정하거나, 복잡하거나, 위험하게 만들지는 않았는가?”
