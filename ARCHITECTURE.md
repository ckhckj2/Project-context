# 척척 구조 정리 — v2.1.65

## 범위와 상태

2026-09-05, index.html에서 실제 로드하는 JavaScript 51개와 CSS 11개를 수집했다.
전체 소스의 정적 위험 지표를 조사하고, 신고된 클릭 경로·버전 쓰기·프로젝트 저장 경로를 집중 검토했다.
모든 분기의 의미 검증, 침투 테스트, Safari/iPad 실기기 검증까지 완료한 것은 아니다.
파일 수나 innerHTML 사용 횟수만으로 취약점이라고 판정하지 않는다.

## 이번에 수정한 확인된 결함

| 결함 | 원인 | 변경 |
| --- | --- | --- |
| 추가 확인을 눌러도 열리지 않음 | v257의 부모 상태와 버튼이 data-cc257-stage를 공유. 부모까지 closest로 잡아 body를 교체 | 부모 상태를 cc257SelectedStage로 분리, contextResult 안의 button만 처리 |
| 같은 단계 클릭 시 내용·포커스 초기화 | 매번 innerHTML 교체 | 입력 키가 같으면 반환, 바뀐 경우 펼침 상태와 탭 포커스 보존 |
| 건축 마스터가 도면 가이드 LV1로 처리 | LV.MAX를 숫자 정규식으로 읽음 | MAX를 가장 깊은 LV4 설명에 매핑 |
| 버전 표시가 바뀜 | 47개 모듈이 설치·타이머·화면 갱신 때 표시를 덮어씀 | 버전 표시와 data-ui-version의 소유자를 index.html 하나로 제한 |
| 기본정보 수정 시 추가 설정 유실 | v230 saveEditor가 기존 필드를 버리고 객체를 새로 생성 | 기존 객체를 보존하고 수정 필드만 덮어쓰기 |
| 외부 링크 보안 적용 범위 누락 | _blank 클릭만 검사, 처리 완료 마커를 신뢰 | 모든 href 클릭·보조 클릭 검사, URL 내 자격증명 거부, 실제 클릭 때 속성 재검사 |

도면 가이드 observer는 자기 컴포넌트 내부 변경을 재처리하지 않는다.
예약된 갱신을 계속 취소하는 debounce도 없앴다.
index.html의 script 사이에 출력되던 문자 그대로의 역슬래시-n을 제거했다.

## 아직 남아 있는 구조적 위험과 다음 작업

| 우선순위 | 위치 | 확인된 구조 / 위험 | 목표 |
| --- | --- | --- | --- |
| 높음 | v21, v216, v217, v219, v223, v227~229, v232, v240~245, v251 | runSearch 재할당과 window/document 캡처가 공존. 최종 행동이 설치 순서에 의존 | 단일 검색 진입점 + 순수 classify 함수 + 의도별 renderer. 문의·정의·비교·구체업무 회귀 케이스를 먼저 고정 |
| 높음 | v250, v252, v254~257 | 여러 observer와 타이머가 contextResult를 보정 | 단일 렌더 순서와 영역별 소유권. 상태 변경 → 모델 계산 → DOM 갱신 한 경로 |
| 높음 | v230, v232, v248 | 프로젝트 배열 읽기·쓰기가 분산. v248은 저장 뒤 타이머로 추가 필드를 붙이고 이름으로 대상을 찾기도 함 | ID 기반 한 번의 저장, 스키마 검증·마이그레이션·오류 반환을 ProjectStore로 통합 |
| 높음 | v2_core 및 저장 관련 모듈 | 일부 localStorage 호출 예외 미처리, 파싱 실패를 빈 배열로 취급 | 손상 데이터는 자동 덮어쓰지 않고 복구 안내. 저장소 차단/용량 초과 테스트 |
| 중간 | v253~257 및 누적 CSS | 스타일과 데이터가 JS 안에 함께 있고 유사 UI 규칙이 중복 | 디자인 토큰·컴포넌트 CSS·콘텐츠 데이터 분리. 스타일 ID 충돌 자동 검사 |
| 중간 | 기존 test_*.js | 문자열 존재 여부 위주 검사. 실제 클릭·DOM 안정성을 충분히 보장하지 못함 | 동작 테스트 확대, 키보드/레벨/단계/프로젝트 조합 브라우저 회귀 |

## 유지 규칙

1. 새 기능용 파일로 이전 이벤트를 가로채는 패치를 추가하지 않는다. 해당 영역 소유 모듈을 수정한다.
2. 상태 속성과 동작 속성을 분리한다. 이벤트는 컴포넌트 루트에 한정한다.
3. 동일한 입력으로 DOM을 다시 바꾸지 않는다. observer가 자기 출력을 반복 수정하지 않게 한다.
4. 화면 버전은 index.html만 관리한다. 각 모듈 VERSION은 모듈의 변경 이력이며 전체 배포 버전이 아니다.
5. 사용자·저장소 값은 신뢰하지 않는다. 텍스트는 textContent 또는 기존 escaping 경로로 렌더한다.
6. CSP를 느슨하게 해서 오류를 숨기지 않는다. eval/동적 코드 실행/새 외부 의존성을 도입하지 않는다.
7. 프로젝트 스키마를 바꿀 때 기존 필드와 데이터 보존 테스트를 먼저 만든다.
8. 관련 파일은 한 커밋으로 반영하고 강제 ref 변경은 하지 않는다.

## 검증 명령

```sh
node tests/run.cjs
node tests/architecture-inventory.cjs
```

runtime-regression은 실제 모듈을 VM/작은 DOM fixture에서 실행한다. 실제 브라우저의 native details 동작은 별도 확인해야 한다.
inventory는 로드되는 파일별 observer·전역 이벤트·HTML 쓰기·저장소·검색 재할당 지표를 출력한다. 보안 인증 도구가 아니다.

## 정리 순서

1. 현재 결함 제거와 테스트 기준선 — 이번 변경.
2. 검색·업무맥락의 이벤트와 렌더링 소유권 통합.
3. 프로젝트 저장소·콘텐츠 모델·스타일 책임 분리.
4. 브라우저 회귀·보안 경계·캐시 검증 후 대체가 끝난 옛 패치 제거.

이후 로드맵은 8(LV4) → 9(퀴즈) → 10(재정리) → 11(일정표)로 유지한다.
이 문서는 2~4단계까지 완료했다는 뜻이 아니다.

## 보안 보장 범위

현재는 정적 사이트이며 CSP의 외부 통신 제한과 로컬 저장 원칙을 유지한다.
localStorage는 암호화된 금고가 아니다. 기밀정보를 입력하지 않는다.
웹 코드 변경만으로 사용자 PC의 랜섬웨어, 악성 확장, GitHub 계정 탈취를 방어했다고 보장할 수 없다.
계정 2FA·브랜치 보호·배포 권한·백업 정책은 이번 코드 검사의 범위 밖이며 별도 확인이 필요하다.
